# 系统应用架构文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 迭代号 | iteration-2603111255 |
| 版本 | v1.0 |
| 日期 | 2026-03-11 |
| 编写人 | TF141-PM |

---

## 1. 现有架构分析

### 1.1 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Frontend (Next.js 15)                           │
│                              Port: 20880                                 │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ AgentChat.tsx                                                     │   │
│  │  ├── 消息列表渲染                                                  │   │
│  │  ├── 流式输出处理 (SSE)                                            │   │
│  │  ├── pendingFiles (待发送文件)                                     │   │
│  │  └── 文件上传按钮                                                  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ FileUploader.tsx                                                  │   │
│  │  ├── 文件选择/拖拽                                                 │   │
│  │  ├── 文件类型/大小验证                                             │   │
│  │  └── 文件预览卡片                                                  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         Backend (FastAPI)                                │
│                              Port: 20881                                 │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ backend.py                                                        │   │
│  │  ├── /api/agents/{name}/chat/stream (流式对话)                     │   │
│  │  ├── /api/agents/{name}/files (文件上传)                          │   │
│  │  ├── /api/agents/{name}/execute (脚本执行 - REST API)              │   │
│  │  └── 全局实例: file_storage_manager, execution_engine             │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│   AgentManager    │   │ FileStorageManager │   │  ExecutionEngine  │
│                   │   │                   │   │                   │
│  AgentInstance    │   │  upload_file()    │   │  execute_script() │
│  AgentEngine      │   │  get_file_path()  │   │  _prepare_workdir │
│                   │   │  list_files()     │   │  _copy_input_files│
│  [问题: 未持有    │   │                   │   │                   │
│   ExecutionEngine]│   └───────────────────┘   └───────────────────┘
└───────────────────┘               │                       │
            │                       │                       │
            ▼                       ▼                       ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│   AgentEngine     │   │ data/files/{agent}│   │ EnvironmentManager│
│                   │   │                   │   │                   │
│  SkillTool        │   │  {agent}/         │   │  Conda 环境管理    │
│  [问题: 未注入    │   │    {file_id}.pdf  │   │  execute_in_env() │
│   execution_engine]│   │                   │   │                   │
└───────────────────┘   └───────────────────┘   └───────────────────┘
            │
            ▼
┌───────────────────┐
│    SkillTool      │
│                   │
│  load_skill()     │ ← 仅返回文档文本 (RAG)
│  execute_script() │ ← 从未被调用!
│                   │
│  [问题: execution │
│   _engine = None] │
└───────────────────┘
```

### 1.2 数据流分析

#### 1.2.1 当前文件上传流程

```
用户选择文件
    │
    ▼
┌───────────────────────────────────────────────┐
│ AgentChat.tsx                                 │
│  pendingFiles: PendingFile[]                  │
│  handleSend() 触发                            │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ 前端: uploadFiles() 函数                      │
│  POST /api/agents/{name}/files                │
│  FormData: multipart/form-data                │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ backend.py: upload_files()                    │
│  file_storage_manager.upload_file()           │
│  返回: [{file_id, filename, ...}]             │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ 前端: 发送对话消息                            │
│  POST /stream/agents/{name}/chat              │
│  body: {message, history, file_ids}           │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ backend.py: chat_stream()                     │
│  构建文件上下文 (文本描述)                    │
│  file_context = "用户上传了以下文件..."        │
│  instance.chat_stream(message, history,       │
│                       file_context)           │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ AgentEngine.stream()                          │
│  system_prompt += file_context                │
│  LLM 看到文件信息文本                          │
│  [问题: LLM 无法真正访问文件]                  │
└───────────────────────────────────────────────┘
```

#### 1.2.2 当前技能执行流程 (失败路径)

```
LLM 调用 load_skill("AB-pdf")
    │
    ▼
┌───────────────────────────────────────────────┐
│ AgentEngine._execute_tool()                   │
│  if tool_name == "load_skill":                │
│    skill_tool.execute(skill_name)             │
│  [问题: 无 execute_skill 分支]                │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ SkillTool.execute()                           │
│  返回 SKILL.md 文档内容                        │
│  LLM "知道"怎么处理PDF，但无法执行             │
└───────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────┐
│ ExecutionEngine                               │
│  execute_script() 方法存在                    │
│  [问题: 从未被调用]                           │
│  仅通过 REST API 暴露，但 LLM 无法调用 REST    │
└───────────────────────────────────────────────┘
```

### 1.3 核心问题定位

#### 问题1: 依赖注入缺失

**文件**: `src/agent_engine.py` 第52-59行

```python
# 当前代码 (有缺陷)
if skill_registry and config.skills:
    self.skill_tool = SkillTool(
        skill_registry=skill_registry,
        skills_dir=self.skills_dir,
        enabled_skills=config.skills
        # 缺失: execution_engine
        # 缺失: agent_name
    )
```

**问题**: `SkillTool` 的 `execution_engine` 属性为 `None`，导致 `get_execute_tool_definition()` 返回 `None`，`execute_skill` 工具从未注册到 LLM。

#### 问题2: 工具调用路由缺失

**文件**: `src/agent_engine.py` 第125-132行

```python
# 当前代码 (有缺陷)
async def _execute_tool(self, tool_name: str, tool_args: Dict) -> str:
    if tool_name == SkillTool.TOOL_NAME and self.skill_tool:
        skill_name = tool_args.get("skill_name") or tool_args.get("skill", "")
        list_files = tool_args.get("list_files", False)
        return await self.skill_tool.execute(skill_name, list_files)
    # 缺失: execute_skill 分支
```

#### 问题3: AgentInstance 未持有全局资源

**文件**: `src/agent_manager.py` 第18-28行

```python
class AgentInstance:
    def __init__(self, config, mcp_registry, skill_registry, skills_dir, model_service_registry):
        # 缺失: execution_engine
        # 缺失: file_storage_manager
        # 缺失: environment_manager
```

---

## 2. 改进方案

### 2.1 目标架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Frontend (Next.js 15)                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ AgentChat.tsx                                                     │   │
│  │  ├── pendingFiles: PendingFile[]                                  │   │
│  │  ├── handleSend()                                                 │   │
│  │  │    ├── uploadFiles() --> file_ids                              │   │
│  │  │    └── sendMessage(file_ids)                                   │   │
│  │  └── 文件预览组件                                                  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         Backend (FastAPI)                                │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ backend.py                                                        │   │
│  │  ├── chat_stream()                                                │   │
│  │  │    ├── 构建 file_context (含 file_ids)                         │   │
│  │  │    └── instance.chat_stream(message, history, file_context)    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│   AgentManager    │   │ FileStorageManager │   │  ExecutionEngine  │
│                   │   │                   │   │                   │
│  持有:            │   │  存储文件          │   │  执行脚本          │
│   execution_engine│   │  返回路径          │   │  复制文件到工作目录 │
│   file_storage    │   │                   │   │                   │
└───────────────────┘   └───────────────────┘   └───────────────────┘
            │                                               │
            ▼                                               ▼
┌───────────────────┐                           ┌───────────────────┐
│   AgentEngine     │                           │ EnvironmentManager│
│                   │                           │                   │
│  持有:            │                           │  Conda 环境管理    │
│   skill_tool      │                           │  隔离执行          │
│   (含 execution   │                           │                   │
│    _engine)       │                           └───────────────────┘
│                   │
│  _execute_tool(): │
│   ├── load_skill  │
│   └── execute_skill│
└───────────────────┘
            │
            ▼
┌───────────────────┐
│    SkillTool      │
│                   │
│  execution_engine │ ← 正确注入
│  agent_name       │ ← 正确注入
│                   │
│  get_all_tool_    │
│  definitions()    │ → 返回 [load_skill, execute_skill]
└───────────────────┘
```

### 2.2 数据流改进

#### 2.2.1 完整的文件上传-执行流程

```
┌─────────────────────────────────────────────────────────────────────┐
│ Step 1: 文件上传                                                    │
│                                                                     │
│ 用户选择 test.pdf                                                   │
│     ↓                                                               │
│ POST /api/agents/skill-test-pdf/files                               │
│     ↓                                                               │
│ FileStorageManager.upload_file()                                    │
│     ↓                                                               │
│ 存储: data/files/skill-test-pdf/{file_id}.pdf                       │
│ 返回: {file_id: "abc123", filename: "test.pdf"}                     │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 2: 发送对话消息                                                │
│                                                                     │
│ POST /stream/agents/skill-test-pdf/chat                             │
│ body: {                                                             │
│   message: "提取这个PDF的文本",                                      │
│   file_ids: ["abc123"]                                              │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 3: 构建文件上下文                                              │
│                                                                     │
│ backend.py chat_stream():                                           │
│   file_context = """                                                │
│   [用户上传了以下文件]                                               │
│   - 文件名: test.pdf                                                │
│     类型: application/pdf                                           │
│     文件ID: abc123                                                  │
│   """                                                               │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 4: LLM 决策并调用工具                                          │
│                                                                     │
│ AgentEngine.stream():                                               │
│   LLM 分析: "需要提取PDF文本"                                        │
│   LLM 决定: 调用 execute_skill                                      │
│   Tool Call: {                                                      │
│     "tool": "execute_skill",                                        │
│     "arguments": {                                                  │
│       "skill_name": "AB-pdf",                                       │
│       "script_name": "extract_text.py",                             │
│       "input_file_ids": ["abc123"]                                  │
│     }                                                               │
│   }                                                                 │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 5: 执行脚本                                                    │
│                                                                     │
│ AgentEngine._execute_tool("execute_skill", args):                   │
│     ↓                                                               │
│ SkillTool.execute_script():                                         │
│     ↓                                                               │
│ ExecutionEngine.execute_script():                                   │
│   ├── get_or_create_environment("skill-test-pdf")                   │
│   │       → 创建/复用 Conda 环境                                    │
│   ├── _prepare_work_dir_with_skill():                               │
│   │       → 复制 skills/builtin/AB-pdf/scripts/* 到工作目录         │
│   ├── _copy_input_files():                                          │
│   │       → 复制 data/files/.../abc123.pdf → workdir/input/         │
│   └── execute_in_environment():                                     │
│           conda run -p env_skill-test-pdf python main.py            │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 6: 返回结果                                                    │
│                                                                     │
│ 脚本输出 (stdout):                                                  │
│   "PDF文本内容: Hello World..."                                     │
│     ↓                                                               │
│ yield {"type": "tool_result", "name": "execute_skill", ...}         │
│     ↓                                                               │
│ LLM 生成最终回答                                                    │
│     ↓                                                               │
│ yield {"type": "content", "content": "您的PDF文件包含..."}           │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.3 关键代码修改点

#### 修改1: AgentInstance 持有执行引擎

**文件**: `src/agent_manager.py`

```python
class AgentInstance:
    def __init__(
        self,
        config: AgentConfig,
        mcp_registry=None,
        skill_registry=None,
        skills_dir=None,
        model_service_registry=None,
        execution_engine=None,  # 新增
        file_storage_manager=None,  # 新增
    ):
        self.execution_engine = execution_engine
        self.file_storage_manager = file_storage_manager
        # ...

    async def initialize(self):
        # ...
        self.engine = AgentEngine(
            self.config,
            self.mcp_manager,
            self.skill_registry,
            self.skills_dir,
            self.model_service_registry,
            self.execution_engine,  # 新增
            self.config.name,  # 新增: agent_name
        )
```

#### 修改2: AgentEngine 接收执行引擎

**文件**: `src/agent_engine.py`

```python
class AgentEngine:
    def __init__(
        self,
        config: AgentConfig,
        mcp_manager=None,
        skill_registry=None,
        skills_dir=None,
        model_service_registry=None,
        execution_engine=None,  # 新增
        agent_name="",  # 新增
    ):
        self.execution_engine = execution_engine
        self.agent_name = agent_name
        # ...

        if skill_registry and config.skills:
            self.skill_tool = SkillTool(
                skill_registry=skill_registry,
                skills_dir=self.skills_dir,
                enabled_skills=config.skills,
                execution_engine=execution_engine,  # 新增
                agent_name=agent_name,  # 新增
            )
```

#### 修改3: 工具调用路由添加 execute_skill

**文件**: `src/agent_engine.py`

```python
async def _execute_tool(self, tool_name: str, tool_args: Dict) -> str:
    # 处理 load_skill
    if tool_name == SkillTool.TOOL_NAME and self.skill_tool:
        skill_name = tool_args.get("skill_name") or tool_args.get("skill", "")
        list_files = tool_args.get("list_files", False)
        return await self.skill_tool.execute(skill_name, list_files)

    # 新增: 处理 execute_skill
    if tool_name == SkillTool.EXECUTE_TOOL_NAME and self.skill_tool:
        return await self.skill_tool.execute_script(
            skill_name=tool_args.get("skill_name"),
            script_name=tool_args.get("script_name", "main.py"),
            arguments=tool_args.get("arguments", []),
            input_file_ids=tool_args.get("input_file_ids", []),
            timeout=tool_args.get("timeout", 60),
        )

    # MCP 工具...
```

#### 修改4: 工具定义注册

**文件**: `src/agent_engine.py`

```python
def _get_tool_definitions(self):
    tools = []

    # 添加 skill 工具
    if self.skill_tool:
        tools.append(self.skill_tool.get_tool_definition())

        # 新增: 添加 execute_skill 工具
        execute_def = self.skill_tool.get_execute_tool_definition()
        if execute_def:
            tools.append(execute_def)

    # MCP 工具...
    return tools
```

#### 修改5: 前端文件上传集成

**文件**: `frontend/src/components/AgentChat.tsx`

```typescript
const uploadFiles = async (files: PendingFile[]): Promise<string[]> => {
  const formData = new FormData();
  files.forEach(f => formData.append('files', f.file));

  const res = await fetch(`/api/agents/${agentName}/files`, {
    method: 'POST',
    body: formData,
  });

  const data = await res.json();
  return data.files.map(f => f.file_id);
};

const handleSend = async () => {
  // 1. 先上传文件
  let fileIds: string[] = [];
  if (pendingFiles.length > 0) {
    fileIds = await uploadFiles(pendingFiles);
    setPendingFiles([]);
  }

  // 2. 发送对话消息
  const res = await fetch(streamUrl, {
    method: 'POST',
    body: JSON.stringify({
      message: userContent,
      history: historyToSend,
      file_ids: fileIds,  // 传递文件ID
    }),
  });
  // ...
};
```

---

## 3. 环境隔离方案

### 3.1 Conda 环境管理

#### 环境命名规范

```
env_{agent_name}
```

例如: `env_skill-test-pdf`, `env_skill-test-doc`

#### 环境生命周期

```
┌─────────────────────────────────────────────────────────────┐
│ Agent 创建                                                   │
│   ↓                                                         │
│ 环境不存在 → 首次执行时创建 (lazy creation)                  │
│   ↓                                                         │
│ 按需安装 Skill 依赖 (pip install)                           │
│   ↓                                                         │
│ 多次执行复用同一环境                                         │
│   ↓                                                         │
│ Agent 删除 → 清理环境 (conda env remove)                    │
└─────────────────────────────────────────────────────────────┘
```

#### 环境元数据

```json
// data/environments/skill-test-pdf.json
{
  "agent_name": "skill-test-pdf",
  "environment_type": "conda",
  "status": "ready",
  "python_version": "3.11",
  "packages": ["pypdf", "pdfplumber", "reportlab"],
  "created_at": "2026-03-11T12:00:00",
  "updated_at": "2026-03-11T12:30:00"
}
```

### 3.2 脚本执行工作目录

```
/tmp/exec_{execution_id}/
├── input/                    # 上传的文件
│   └── test.pdf
├── output/                   # 脚本输出 (可选)
├── main.py                   # Skill 主脚本
├── helpers/                  # 辅助模块
└── requirements.txt          # 依赖声明
```

---

## 4. API 接口规范

### 4.1 文件上传

```http
POST /api/agents/{name}/files
Content-Type: multipart/form-data

files: [File, File, ...]

Response 200:
{
  "files": [
    {
      "file_id": "abc123",
      "filename": "test.pdf",
      "file_size": 102400,
      "mime_type": "application/pdf"
    }
  ]
}
```

### 4.2 流式对话 (带文件)

```http
POST /api/agents/{name}/chat/stream
Content-Type: application/json

{
  "message": "提取PDF文本",
  "history": [...],
  "file_ids": ["abc123"]
}

Response 200 (SSE):
data: {"type": "thinking", "content": "正在分析..."}
data: {"type": "tool_call", "name": "execute_skill", "args": {...}}
data: {"type": "tool_result", "name": "execute_skill", "result": "..."}
data: {"type": "content", "content": "您"}
data: {"type": "content", "content": "的"}
data: {"type": "content", "content": "PDF..."}
data: {"type": "metrics", "first_token_latency": 500, ...}
```

### 4.3 脚本执行 (Tool Call)

```json
{
  "tool": "execute_skill",
  "arguments": {
    "skill_name": "AB-pdf",
    "script_name": "extract_text.py",
    "input_file_ids": ["abc123"],
    "arguments": ["--page-range", "1-5"],
    "timeout": 60
  }
}
```

---

## 5. 前端组件架构

### 5.1 AgentChat 组件状态

```typescript
interface AgentChatState {
  messages: ChatMessage[];
  isRunning: boolean;
  inputValue: string;
  pendingFiles: PendingFile[];  // 待发送文件
  hasError: boolean;
}

interface PendingFile {
  id: string;
  file: File;
  name: string;
  size: number;
  type: string;
}

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  attachments?: FileAttachment[];  // 已上传的文件
  toolCalls?: ToolCall[];
  thinking?: string;
}
```

### 5.2 文件上传组件

```tsx
// FileUploader.tsx
interface FileUploaderProps {
  files: PendingFile[];
  onFilesChange: (files: PendingFile[]) => void;
  maxFileSize?: number;
  allowedTypes?: string[];
  disabled?: boolean;
}

// 使用示例
<FileUploader
  files={pendingFiles}
  onFilesChange={setPendingFiles}
  maxFileSize={50 * 1024 * 1024}  // 50MB
  allowedTypes={['.pdf', '.docx', '.xlsx']}
  disabled={isRunning}
/>
```

---

## 6. 错误处理

### 6.1 环境创建失败

```python
try:
    env = await environment_manager.get_or_create_environment(agent_name)
except EnvironmentError as e:
    yield {
        "type": "error",
        "content": f"环境创建失败: {str(e)}"
    }
    return
```

### 6.2 脚本执行超时

```python
try:
    result = await execution_engine.execute_script(..., timeout=60)
except asyncio.TimeoutError:
    yield {
        "type": "tool_result",
        "name": "execute_skill",
        "result": "执行超时 (60秒)"
    }
```

### 6.3 文件上传失败

```typescript
try {
  fileIds = await uploadFiles(pendingFiles);
} catch (error) {
  setHasError(true);
  // 显示错误提示
  return;
}
```

---

## 7. 性能考虑

### 7.1 环境预热

```python
# 可选: Agent 创建时预热环境
async def preheat_environment(agent_name: str, skills: List[str]):
    """预先创建环境并安装依赖"""
    env = await environment_manager.get_or_create_environment(agent_name)
    for skill in skills:
        requirements = get_skill_requirements(skill)
        if requirements:
            await environment_manager.install_packages(agent_name, requirements)
```

### 7.2 并发控制

```python
class ExecutionEngine:
    MAX_CONCURRENT_PER_AGENT = 3  # 每个 Agent 最多 3 个并发执行
    _semaphores: Dict[str, asyncio.Semaphore] = {}
```

### 7.3 工作目录清理

```python
# 执行完成后自动清理
self._cleanup_work_dir(work_dir)
```

---

## 8. 测试验证

### 8.1 单元测试

```python
# tests/test_execution_integration.py

async def test_skill_tool_has_execution_engine():
    """验证 SkillTool 正确接收 execution_engine"""
    skill_tool = SkillTool(
        skill_registry=mock_registry,
        skills_dir=Path("skills"),
        enabled_skills=["AB-pdf"],
        execution_engine=mock_engine,
        agent_name="test-agent"
    )
    assert skill_tool.execution_engine is not None

async def test_execute_skill_tool_call():
    """验证 execute_skill 工具被正确调用"""
    result = await agent_engine._execute_tool("execute_skill", {
        "skill_name": "AB-pdf",
        "input_file_ids": ["abc123"]
    })
    assert "Execution Result" in result
```

### 8.2 集成测试

```python
async def test_full_flow():
    """端到端测试"""
    # 1. 上传文件
    file_info = await file_storage.upload_file("test-agent", content, "test.pdf")

    # 2. 执行对话
    events = []
    async for event in agent.chat_stream("提取文本", [], file_context):
        events.append(event)

    # 3. 验证结果
    assert any(e["type"] == "tool_call" and e["name"] == "execute_skill" for e in events)
    assert any(e["type"] == "content" for e in events)
```

---

## 9. 迁移路径

### 9.1 向后兼容

- 现有 `load_skill` 工具保持不变
- 新增 `execute_skill` 工具为可选
- 如果 Skill 没有可执行脚本，不暴露 `execute_skill`

### 9.2 数据迁移

- 现有 Agent 配置无需修改
- 现有文件存储结构保持不变
- 环境管理为增量功能

---

## 10. 附录

### 10.1 相关文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/agent_engine.py` | 修改 | 添加 execute_skill 路由 |
| `src/agent_manager.py` | 修改 | 持有 execution_engine |
| `src/skill_tool.py` | 无需修改 | 已有完整实现 |
| `src/execution_engine.py` | 无需修改 | 已有完整实现 |
| `src/environment_manager.py` | 无需修改 | 已有完整实现 |
| `src/file_storage_manager.py` | 无需修改 | 已有完整实现 |
| `backend.py` | 无需修改 | 已有完整实现 |
| `frontend/src/components/AgentChat.tsx` | 修改 | 添加文件上传流程 |
| `frontend/src/lib/fileApi.ts` | 无需修改 | 已有完整实现 |

### 10.2 依赖关系图

```
backend.py
    ├── AgentManager
    │       ├── ExecutionEngine (全局)
    │       └── FileStorageManager (全局)
    │
    └── AgentInstance
            └── AgentEngine
                    └── SkillTool
                            └── ExecutionEngine
                                    └── EnvironmentManager
```
